<%= form_with(model: bible, local: true) do |form| %>
  <% if bible.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bible.errors.count, "error") %> prohibited this bible from being saved:</h2>

      <ul>
        <% bible.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :bibleId %>
    <%= form.text_field :bibleId %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
