<h1>Editing Article</h1>
<% buttons = []
buttons << back_button(:back)
 buttons << view_button(@article) %>
 <%= toolbar(buttons)%>
<%= render 'form', article: @article %>
<script>
  // debugger
      ClassicEditor
              .create( document.querySelector( '#editor' ) )
              .then( editor => {
                      console.log( editor );
                        // $(".ck-content").css("height","400px")
              } )
              .catch( error => {
                      console.error( error );
              } );

</script>
