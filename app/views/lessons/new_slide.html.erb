<%= render 'create_slide_form', lesson: @lesson %>
<script type="text/javascript">
  $("body").ready(()=>{
    ClassicEditor
            .create( document.querySelector( '#editor' ),{
              ckfinder: {
                  // Upload the images to the server using the CKFinder QuickUpload command.
                  uploadUrl: '<%= lesson_upload_url(@lesson) %>',
              }
            } )
            .then( editor => {
                    console.log( editor );
                      // $(".ck-content").css("height","400px")
            } )
            .catch( error => {
                    console.error( error );
            } );
  })
</script>
